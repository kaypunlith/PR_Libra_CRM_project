<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ut.nlSystemAPi.mapper.primary.GroupMapper">

  <sql id="BASE_COLUMN_LIST">
    *,
    DATE_FORMAT(created, "%d-%b-%Y") AS modifiedDate
  </sql>

  <sql id="BASE_FROM">
    FROM egroups
  </sql>

  <sql id="BASE_WHERE">
    WHERE is_active = 1
    <if test="filter.searchText != null">AND egroups.name LIKE CONCAT("%", #{filter.searchText}, "%")</if>
  </sql>

  <select id="getList" parameterType="com.ut.nlSystemAPi.model.base.Filter" resultMap="GroupResponse">
    SELECT
    <include refid="BASE_COLUMN_LIST"/>
    <include refid="BASE_FROM"/>
    <include refid="BASE_WHERE"/>
    ORDER BY
    <choose>
        <when test="filter.orderBy != null">
            ${filter.orderBy}
        </when>
        <otherwise>
            id DESC
        </otherwise>
    </choose>
    <if test="filter.page != null and filter.rowsPerPage != null">LIMIT #{filter.page}, #{filter.rowsPerPage}</if>
  </select>

  <select id="getOne" parameterType="java.lang.Long" resultMap="GroupResponse">
    SELECT
    <include refid="BASE_COLUMN_LIST"/>
    FROM egroups
    WHERE is_active = 1 AND id = #{id}
  </select>

  <select id="listFilterGroup" parameterType="com.ut.nlSystemAPi.model.base.Filter" resultMap="GroupResponse">
    SELECT *
    FROM egroups
    WHERE is_active = 1 AND id IN (SELECT group_id FROM departments WHERE is_active = 1
    AND id IN (SELECT department_id FROM user_departments WHERE user_id = #{userId}))
  </select>

  <select id="checkDuplicate" resultType="java.lang.Long">
    SELECT COUNT(id)
    FROM departments
    WHERE is_active = 1 AND name = #{name}
    <if test="id != null"> AND id != #{id} </if>
  </select>

  <insert id="insert" parameterType="com.ut.nlSystemAPi.model.request.GroupRequest" keyProperty="groupRequest.id">
    INSERT INTO egroups (
      name,
      description,
      created,
      created_by,
      is_active
    ) VALUES (
      #{groupRequest.name},
      #{groupRequest.description},
      NOW(),
      #{groupRequest.createdBy},
      #{groupRequest.isActive}
    )
  </insert>

  <update id="update" parameterType="com.ut.nlSystemAPi.model.request.GroupUpdateRequest">
    UPDATE egroups SET
    <if test="groupUpdateRequest.name != null">name = #{groupUpdateRequest.name},</if>
    <if test="groupUpdateRequest.description != null">description = #{groupUpdateRequest.description},</if>
    modified = NOW(),
    modified_by = #{groupUpdateRequest.modifiedBy}
    WHERE id = #{groupUpdateRequest.id}
  </update>

  <delete id="delete" parameterType="java.lang.Long">
    UPDATE egroups SET
      is_active = 2,
      modified = NOW(),
      modified_by = #{userId}
    WHERE id = #{id}
  </delete>

</mapper>
