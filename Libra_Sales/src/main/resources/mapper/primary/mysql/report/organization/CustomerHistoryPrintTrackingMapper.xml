<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ut.nlSystemAPi.mapper.primary.CustomerHistoryPrintTrackingMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO customer_history_print_tracking (type, reference_id, print_count)
        VALUES (#{type}, #{referenceId}, 1)
    </insert>
    
    <update id="updateStatus">
        UPDATE customer_history_print_tracking
        SET
            print_count = IFNULL(print_count, 0) + 1
        WHERE type = #{type} AND reference_id = #{referenceId}
    </update>

    <select id="findByTypeAndReferenceId" resultType="com.ut.nlSystemAPi.model.entity.Organization.CustomerHistoryPrintTracking">
        SELECT * FROM customer_history_print_tracking
        WHERE type = #{type} AND reference_id = #{referenceId}
        LIMIT 1
    </select>
</mapper>
