<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ut.nlSystemAPi.mapper.primary.AccountClosingDateMapper">

  <select id="getList" resultType="java.lang.String">
      SELECT date FROM account_closing_dates WHERE 1 ORDER BY id DESC LIMIT 1
  </select>

  <insert id="insert">
      INSERT INTO account_closing_dates(
          date,
          created,
          created_by
      ) VALUES (
           #{accountClosingDate},
           NOW(),
           #{userId}
        );
  </insert>

</mapper>